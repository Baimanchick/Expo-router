import { View, Text, Button } from "react-native";
import React, { useReducer } from "react";

const Page = () => {
  return (
    <View>
      <Text>Register Users</Text>
    </View>
  );
};

export default Page;
